package com.gmmapowell.bytecode;

public class GenericAnnotator {
	/*
	StringBuilder sb = new StringBuilder();
	sb.append("()");
	sb.append(JavaInfo.map(S.list));
	sb.deleteCharAt(sb.length()-1);
	sb.append("<");
	sb.append(JavaInfo.map(r.getReturnType()));
	sb.append(">;");
	getter.addAttribute("Signature", sb.toString());
	*/
	/*
	StringBuilder sb = new StringBuilder();
	sb.append("(");
	if (s != null)
		sb.append(JavaInfo.map(S.string));
	sb.append(JavaInfo.map(type));
	sb.deleteCharAt(sb.length()-1);
	sb.append("<");
	sb.append(JavaInfo.map(type));
	sb.append(">;)V");
	meth.addAttribute("Signature", sb.toString());
	*/

}
